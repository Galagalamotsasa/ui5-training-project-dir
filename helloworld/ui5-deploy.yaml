# yaml-language-server: $schema=https://sap.github.io/ui5-tooling/schema/ui5.yaml.json

specVersion: "4.0"
metadata:
  name: student04.sap.training.helloworld
type: application
builder:
  resources:
    excludes:
      - /test/**
      - /localService/**
  customTasks:
    - name: deploy-to-abap
      afterTask: generateCachebusterInfo
      configuration:
        target:
          destination: Gateway
          url: http://61.97.134.35:8000
          client: '100'
        app:
          name: ZHELLOWORLD_04
          description: TEST Deploy
          package: $TMP
          transport: ''
        exclude:
          - /test/
