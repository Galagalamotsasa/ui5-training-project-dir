# yaml-language-server: $schema=https://sap.github.io/ui5-tooling/schema/ui5.yaml.json

specVersion: "4.0"
metadata:
  name: com.sap.project204
type: application
builder:
  resources:
    excludes:
      - /test/**
      - /localService/**
  customTasks:
    - name: deploy-to-abap
      afterTask: generateCachebusterInfo
      configuration:
        target:
          destination: Gateway
          url: http://61.97.134.35:8000
          client: '100'
        app:
          name: ZPROJECT2_04
          description: deploy test
          package: ZFIORI_04
          transport: EFXK901885
        exclude:
          - /test/
